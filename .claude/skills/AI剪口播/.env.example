# 复制本文件为 .env（就放在 skill 目录下即可），填入你自己的火山引擎 API Key。
# 也可以不建文件，直接 export VOLCENGINE_API_KEY=...（任何 agent / 安装位置都认）。
# 查找顺序见 scripts/lib/load_api_key.sh。真实 .env 已被 .gitignore 忽略，不会进仓库。
#
# 申请地址（新版控制台，生成「一个」API Key 即可，所有引擎共用）：
#   https://console.volcengine.com/speech/new/overview
#
# 还需在控制台开通两个资源（各有 20h 免费额度，独立抵扣）：
#   · 录音文件识别 - 极速版  (volc.bigasr.auc_turbo)
#   · 录音文件识别 - 标准版  (volc.bigasr.auc)
# 默认 auto 模式两者轮流，吃满 ≈40h；只开一个就用 --flash 或 --v3-standard。

VOLCENGINE_API_KEY=your_api_key_here
