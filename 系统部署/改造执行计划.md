# 系统改造执行计划

**制定日期**：2026-04-29
**总体进度**：约 25%

---

## 概述

本计划将完成完整的系统改造，涵盖数据层增强、Skill Bridge优化、配置层新建和旧代码清理五个阶段。

---

## 阶段一：数据层增强（P0）

### 任务1.1：画像增强 - 新增字段
**优先级**：P0
**修改文件**：`系统部署/services/portrait_generator.py`

**新增字段到 Portrait dataclass**：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `language_style` | str | "" | 人群语言风格：口语化/专业术语/情绪化 |
| `crowd_perspective` | str | "" | 人群视角：第一人称/第三人称 |
| `age_range` | str | "" | 年龄范围 |
| `pain_point_level` | str | "medium" | 痛点强度：high/medium/low |
| `decision_stage` | str | "consideration" | 决策阶段：awareness/consideration/decision |

**修改位置**：第33-67行 `Portrait` dataclass

**验证方式**：
- 单元测试验证新字段生成
- 检查 portrait_generator.json 配置是否需要同步更新

---

### 任务1.2：选题增强 - 新增字段
**优先级**：P0
**修改文件**：`系统部署/services/topic_library_generator.py`

**新增字段到 Topic dataclass**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `scene_details` | List[Dict] | 场景细分：[{"scene": "...", "trigger": "...", "emotion": "..."}] |
| `core_value` | str | 核心卖点：一句话说明选题价值 |
| `content_format` | str | 内容形式：种草/测评/教程/对比 |
| `emotion_curve` | str | 情绪曲线：引流→共情→干货→转化 |

**同时更新**：
- `_build_topic_prompt()` 方法中的Prompt模板
- `topic_generator.json` 配置中的 output_schema

---

### 任务1.3：关键词库增强 - 新增字段
**优先级**：P0
**修改文件**：`系统部署/services/keyword_library_generator.py`

**新增字段到返回结构**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `geo_score` | Dict | 地理评分：{"region": "...", "score": 85, "reason": "..."} |
| `trust_keywords` | List[str] | 信任关键词（从现有结构提取到顶层） |
| `data_sources` | List[str] | 数据来源：["行业报告", "用户调研"] |

**同时更新**：
- `KeywordLibraryResult.to_dict()` 方法
- `keyword_library_generator.json` 配置

---

## 阶段二：Skill Bridge增强（P0）

### 任务2.1：新建 graphic_skill.json
**优先级**：P0
**新建文件**：`系统部署/services/skill_bridge/config/graphic_skill.json`

**结构参考**：`content_generator.json` + `portrait_generator.json`

**核心步骤**：
1. step_emotion_curve：情绪曲线规划
2. step_slide_design：分镜设计
3. step_content_write：内容撰写
4. step_visual_spec：视觉规范

**数据流**：
- 输入：`topic`, `portrait`, `keyword_library`
- 输出：`slides`, `visual_guide`, `emotion_curve`

---

### 任务2.2：增强 data_mapper.py
**优先级**：P0
**修改文件**：`系统部署/services/skill_bridge/data_mapper.py`

**新增方法**：

```python
def map_portrait_to_content(portrait: dict) -> dict:
    """画像到内容的字段映射"""

def map_topic_to_content(topic: dict) -> dict:
    """选题到内容的字段映射"""

def map_keyword_to_content(keyword_library: dict) -> dict:
    """关键词库到内容的字段映射"""
```

---

## 阶段三：配置层新建（P1）

### 任务3.1：新建 emotion_curves.py
**优先级**：P1
**新建文件**：`系统部署/content_templates/emotion_curves.py`

**内容结构**：
```python
EMOTION_CURVES = {
    "种草型": {...},
    "干货型": {...},
    "测评型": {...},
    "对比型": {...},
    "故事型": {...},
    "悬念型": {...},
    "温情型": {...}
}
```

**每种类型包含**：
- `stages`: 情绪阶段列表
- `ratio`: 各阶段字数比例
- `keywords`: 情绪触发关键词
- `visual_hints`: 画面提示

---

### 任务3.2：新建 visual_guides.py
**优先级**：P1
**新建文件**：`系统部署/content_templates/visual_guides.py`

**内容结构**：
```python
VISUAL_GUIDES = {
    "warm_tone": {...},
    "professional_tone": {...},
    "fresh_tone": {...},
    "luxury_tone": {...},
    "casual_tone": {...}
}
```

**每种风格包含**：
- `colors`: 色值列表（HEX）
- `light`: 光线说明
- `scene`: 推荐场景
- `font_pairing`: 字体搭配
- `icon_style`: 图标风格

---

### 任务3.3：新建 expert_reviewer.py
**优先级**：P1
**新建文件**：`系统部署/services/expert_reviewer.py`

**5维评审体系**：

| 维度 | 说明 | 权重 |
|------|------|------|
| `psychology_compliance` | 心理合规：不利用焦虑/恐惧 | 20% |
| `emotion_resonance` | 情绪共鸣：能否引发情感反应 | 20% |
| `trust_building` | 信任建立：证据/权威/真实感 | 25% |
| `value_delivery` | 价值传递：干货/实用/可操作 | 25% |
| `action_guide` | 行动引导：明确的CTA | 10% |

---

## 阶段四：长文/短视频适配（P1）

**状态**：✅ 已完成

- `long_text_generator.json` - 已存在
- `video_script_generator.json` - 已存在

---

## 阶段五：旧代码清理（P2）

### 任务5.1：创建公共Prompt约束模块
**优先级**：P2
**新建文件**：`系统部署/services/prompt_constraints.py`

**内容**：
```python
# JSON输出约束
JSON_OUTPUT_CONSTRAINT = "请严格JSON输出，不要包含其他内容。"

# System Prompt 模板
SYSTEM_PROMPTS = {
    "content_expert": "你是一位资深的内容策划专家...",
    "keyword_expert": "你是关键词库生成专家...",
    "portrait_expert": "你是用户画像分析专家..."
}

# GEO评分专家Prompt
GEO_EVALUATION_PROMPTS = {...}

# JSON格式示例
JSON_EXAMPLES = {...}
```

**需更新的文件**：
- `topic_generator.py`
- `topic_library_generator.py`
- `keyword_topic_generator.py`
- `content_generator.py`
- `content_quality_scorer.py`

---

### 任务5.2：统一JSON解析器
**优先级**：P2
**新建文件**：`系统部署/services/json_parser.py`

**封装方法**：
```python
class JSONParser:
    @staticmethod
    def parse_with_fallback(text: str, schema: dict) -> dict:
        """7层降级解析"""

    @staticmethod
    def extract_json_block(text: str) -> str:
        """从markdown提取JSON"""

    @staticmethod
    def fix_common_errors(text: str) -> str:
        """修复常见JSON错误"""
```

**需更新的文件**：
- `topic_library_generator.py`（移除内嵌解析逻辑）
- `portrait_generator.py`（复用统一解析器）
- 其他生成器

---

### 任务5.3：统一标题生成入口
**优先级**：P2
**修改文件**：`系统部署/services/title_generator.py`

**操作**：
1. 确保 `title_generator.py` 是唯一的标题生成入口
2. 从其他文件移除重复的标题生成逻辑
3. 添加便捷导出函数

---

### 任务5.4：统一评分逻辑入口
**优先级**：P2
**修改文件**：`系统部署/services/content_quality_scorer.py`

**操作**：
1. 增强 `content_quality_scorer.py` 作为唯一评分入口
2. 评估是否需要保留 `content_quality_optimizer.py`
3. 如需保留，明确职责分工

---

## 执行顺序

```
P0 任务（并行执行）：
├── 任务1.1：画像增强
├── 任务1.2：选题增强
├── 任务1.3：关键词库增强
├── 任务2.1：新建 graphic_skill.json
└── 任务2.2：增强 data_mapper.py

P1 任务（顺序执行，在P0完成后）：
├── 任务3.1：新建 emotion_curves.py
├── 任务3.2：新建 visual_guides.py
└── 任务3.3：新建 expert_reviewer.py

P2 任务（最后执行，在P1完成后）：
├── 任务5.1：创建 prompt_constraints.py
├── 任务5.2：统一 json_parser.py
├── 任务5.3：统一标题入口
└── 任务5.4：统一评分入口
```

---

## 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 修改 Portrait 影响现有流程 | 高 | 添加字段有默认值，兼容旧逻辑 |
| Skill配置格式错误 | 中 | 参考现有配置文件格式 |
| 清理代码影响其他模块 | 高 | 分步骤执行，每步验证 |

---

## 验收标准

1. ✅ 所有新增字段有合理默认值，不破坏现有功能
2. ✅ Skill配置文件可通过 registry 正常加载
3. ✅ 情绪曲线和视觉指南可被内容生成器引用
4. ✅ 公共模块可被其他服务正确导入使用
5. ✅ 清理后的代码功能与清理前一致
