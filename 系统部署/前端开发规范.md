# 前端开发规范（UI 可读性相关）

## 强制规定：禁止使用 `bg-*` 的 Badge

**所有 Badge 组件必须使用 `text-bg-*` 形式，禁止使用 `bg-*` 形式**

- 错误：`<span class="badge bg-primary">标签</span>`
- 正确：`<span class="badge text-bg-primary">标签</span>`

**原因**：`bg-*` 只设置背景色，文字颜色继承父元素。在暗黑模式下可能导致文字看不清。

## 文字可读性

1. **禁止使用深色底色的徽章类样式显示正文信息或统计数字**
   - 包括但不限于：`badge bg-primary`、`badge bg-secondary`、`badge bg-success` 等。
   - 统计数字、标签、计数器，统一使用：`text-muted small` 或 `text-muted small border rounded px-2 py-1` 等**无实色背景**样式。

2. **可点击按钮才允许使用实色背景**
   - 按钮使用 `btn-*` 系列，例如：`btn-primary`、`btn-success`。
   - 纯展示型文字统一避免有色背景，仅用文字颜色和细边框区分层级。

3. **以后新页面/组件必须遵守以上规则**
   - 发现历史代码中有使用 `badge bg-*` 显示正文或数字的场景，优先改为：
     - 统计：`<span class="text-muted small">xx 条规则</span>`
     - 标签：`<span class="text-muted small border rounded px-2 py-1">标签</span>`
     - 评分：`<span class="badge text-bg-success">8分</span>`

## 暗黑模式兼容性

1. **避免使用硬编码的颜色**
   - 使用 CSS 变量：`var(--color-bg-primary)`、`var(--text-muted)` 等
   - 避免使用纯黑色 `#000` 或纯白色 `#fff` 作为背景

2. **图片和图标注意对比度**
   - 确保图标在不同主题下都清晰可见
   - 使用 `text-*` 类而非硬编码颜色

3. **测试时切换暗黑模式检查**

## 弹出框规范

1. **弹出框严禁使用滑动条**
   - 弹出框必须足够大，能够完整展示所有信息
   - 使用 `max-width` 和合理的布局确保内容完整显示
   - 禁止在弹出框内使用 `overflow-y: auto` 产生滑动条

2. **弹出框内严禁继续弹出**
   - 禁止在模态框内再次触发新的模态框
   - 所有交互应在当前弹出框内完成，或使用其他交互方式（如展开行内内容）

3. **弹出框尺寸规范**
   - 大弹出框：`max-width: 1000px` 或更大
   - 中弹出框：`max-width: 800px`
   - 小弹出框：`max-width: 500px`
   - 高度不要使用 `max-height` 限制，让内容自然撑开

## 代码检查清单

- [ ] 搜索所有 `badge bg-` 替换为 `badge text-bg-`
- [ ] 搜索所有 `badge bg-light` 替换为 `badge text-bg-light`
- [ ] 统计数字优先使用 `text-muted small` 而非 badge
- [ ] 在暗黑模式下测试所有带颜色的元素
- [ ] 检查所有弹出框无滑动条
- [ ] 检查无弹出框内弹出新弹框的情况

